# CAPI Gateway — env (JANGAN commit .env; set di Coolify Environment Variables)
PORT=4333
HOST=0.0.0.0
# comma-separated raw keys (simpan key asli di secrets, 1 per tenant)
CLIENT_KEYS=CHANGE_ME_SARIGLASS_KEY
META_PIXEL_ID=28227479816902794
META_SYS_TOKEN=CHANGE_ME (secrets/VPS-CREDENTIALS.md repo sariglassbangunan)
# kosongkan di produksi setelah test_event_code selesai dipakai
META_TEST_CODE=
SGB_URL=https://sgb.beriklan.co.id
SGB_INGEST_KEY=CHANGE_ME (ingest key tenant sariglass)
